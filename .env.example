# Environment Variables for Taskfile

# Your GitHub username (required for GHCR publishing)
export GITHUB_USERNAME="your-github-username"

# GitHub Personal Access Token with packages:write permission
# Get it from: https://github.com/settings/tokens
# Required scopes: write:packages, read:packages
export GITHUB_TOKEN="your-github-token"

# Optional: Override registry (default is ghcr.io)
# export REGISTRY="ghcr.io"
